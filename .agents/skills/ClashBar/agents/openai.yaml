interface:
  display_name: "ClashBar"
  short_description: "Repo-specific patterns and workflows for ClashBar"
  default_prompt: "Use the ClashBar repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true